<Window x:Class="WpfTests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Helium.Wpf.Controls;assembly=Helium.Wpf"
        xmlns:local="clr-namespace:WpfTests"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800"
        Background="{DynamicResource Helium.Dark.Brushes.Background1}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Margin="10" Content="Test Singleton" Command="{Binding TestSingletonCommand}" />
            <Button Margin="10" Content="Test Scoped" Command="{Binding TestScopedCommand}" />
            <Button Margin="10" Content="Test Transiant blah blah blah" Command="{Binding TestTransiantCommand}" />
            <controls:RedButton Content="TEST" Margin="10" />
            <TextBox Margin="10" />
        </StackPanel>
    </Grid>
</Window>
