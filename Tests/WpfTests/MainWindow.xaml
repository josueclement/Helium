<Window x:Class="WpfTests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Helium.Wpf.Controls;assembly=Helium.Wpf"
        xmlns:local="clr-namespace:WpfTests"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Margin="10" Content="Test Singleton" Width="100" Command="{Binding TestSingletonCommand}" />
            <Button Margin="10" Content="Test Scoped" Width="100" Command="{Binding TestScopedCommand}" />
            <Button Margin="10" Content="Test Transiant" Width="100" Command="{Binding TestTransiantCommand}" />
            <controls:RedButton Content="TEST" />
        </StackPanel>
    </Grid>
</Window>
